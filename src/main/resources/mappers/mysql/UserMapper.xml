<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.web.mappers.UserMapper">
	<resultMap id="BaseResultMap" type="com.web.entity.User">
		<id column="id" property="id" />
		<result column="user_name" property="userName" />
		<result column="real_name" property="realName" />
		<result column="password" property="password" />
		<result column="identificationNo" property="identificationNo" />
		<result column="mobile" property="mobile" />
		<result column="sex" property="sex" />
		<result column="enabled" property="enabled" />
		<result column="create_Name" property="createName" />
		<result column="create_Date" property="createDate" />
		<result column="remark" property="remark" />
		<result column="deleted" property="deleted" />
		<result column="superAdmin" property="superAdmin" />
		<result column="update_Name" property="updateName" />
		<result column="update_date" property="updateDate" />
	</resultMap>
	<sql id="Example_Where_Clause" >
		<!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
		<where >
			<foreach collection="oredCriteria" item="criteria" separator="or" >
				<if test="criteria.valid" >
					<trim prefix="(" suffix=")" prefixOverrides="and" >
						<foreach collection="criteria.criteria" item="criterion" >
							<choose >
								<when test="criterion.noValue" >
									and ${criterion.condition}
								</when>
								<when test="criterion.singleValue" >
									and ${criterion.condition} #{criterion.value}
								</when>
								<when test="criterion.betweenValue" >
									and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
								</when>
								<when test="criterion.listValue" >
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
										#{listItem}
									</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>
	<sql id="Update_By_Example_Where_Clause" >
		<!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
		<where >
			<foreach collection="example.oredCriteria" item="criteria" separator="or" >
				<if test="criteria.valid" >
					<trim prefix="(" suffix=")" prefixOverrides="and" >
						<foreach collection="criteria.criteria" item="criterion" >
							<choose >
								<when test="criterion.noValue" >
									and ${criterion.condition}
								</when>
								<when test="criterion.singleValue" >
									and ${criterion.condition} #{criterion.value}
								</when>
								<when test="criterion.betweenValue" >
									and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
								</when>
								<when test="criterion.listValue" >
									and ${criterion.condition}
									<foreach collection="criterion.value" item="listItem" open="(" close=")" separator="," >
										#{listItem}
									</foreach>
								</when>
							</choose>
						</foreach>
					</trim>
				</if>
			</foreach>
		</where>
	</sql>
	<sql id="Base_Column_List" >
		<!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
		id, user_name, real_name, password, identificationNo, mobile, sex, enabled, create_Name,
		create_Date, remark, deleted, superAdmin, update_Name, update_date
	</sql>

	<select id="selectByExample" resultMap="BaseResultMap" parameterType="com.web.example.UserExample" >
		<!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
		select
		<if test="distinct" >
			distinct
		</if>
		<include refid="Base_Column_List" />
		from t_s_user
		<if test="_parameter != null" >
			<include refid="Example_Where_Clause" />
		</if>
		<if test="orderByClause != null" >
			order by ${orderByClause}
		</if>
	</select>

	<select id="countByExample" parameterType="com.web.example.UserExample" resultType="java.lang.Integer" >
		<!--
          WARNING - @mbggenerated
          This element is automatically generated by MyBatis Generator, do not modify.
        -->
		select count(*) from t_s_user
		<if test="_parameter != null" >
			<include refid="Example_Where_Clause" />
		</if>
	</select>

	<select id="selectOneById" resultMap="BaseResultMap">
		select * from t_s_user
		where id = #{id}
	</select>
	<select id="getUserByName" resultMap="BaseResultMap">
		select * from t_s_user
		where user_name = #{username}
	</select>
	<select id="getAll" resultMap="BaseResultMap">
		select * from t_s_user
	</select>
	<insert id="save" flushCache="true" parameterType="com.web.entity.User">
		INSERT INTO
		t_s_user (
		id,
		user_name,
		real_name,
		password,
		identificationNo,
		mobile,
		sex,
		enabled,
		create_Name,
		create_Date,
		remark,
		superAdmin
		)
		VALUES (
		#{id},
		#{userName},
		#{realName},
		#{password},
		#{identificationNo},
		#{mobile},
		<choose>
			<when test="sex !=null and sex !=''">
				#{sex},
			</when>
			<otherwise>
				0,
			</otherwise>
		</choose>
		<choose>
			<when test="enabled !=null and enabled !=''">
				#{enabled},
			</when>
			<otherwise>
				0,
			</otherwise>
		</choose>
		#{createName},
		now(),
		#{remark},
		<choose>
			<when test="superAdmin != null and superAdmin !=''">
				#{superAdmin}
			</when>
			<otherwise>
				0
			</otherwise>
		</choose>
		)
	</insert>
	<update id="update" flushCache="true" parameterType="com.web.entity.User">
		UPDATE
		t_s_user SET
		user_name=#{user_name},
		real_name=#{realName},
		password=#{password},
		update_Name=#{updateName},
		update_date=#{updateDate},
		<choose>
		<when test="identificationNo !=null and identificationNo !=''">
		identificationNo=#{identificationNo},
		</when>
		<otherwise>
		identificationNo=null,
		</otherwise>
		</choose>
		<choose>
		<when test="mobile !=null and mobile !=''">
		mobile=#{mobile},
		</when>
		<otherwise>
		mobile=null,
		</otherwise>
		</choose>
		<choose>
		<when test="sex !=null and sex !=''">
		sex=#{sex},
		</when>
		<otherwise>
		sex=0,
		</otherwise>
		</choose>
		<choose>
		<when test="enabled !=null and enabled !=''">
		enabled=#{enabled},
		</when>
		<otherwise>
		enabled=0,
		</otherwise>
		</choose>
		<choose>
		<when test="remark !=null and remark !=''">
		remark=#{remark},
		</when>
		<otherwise>
		remark=null,
		</otherwise>
		</choose>
		<choose>
		<when test="superAdmin !=null and superAdmin !=''">
		superAdmin=#{superAdmin}
		</when>
		<otherwise>
		superAdmin=0
		</otherwise>
		</choose>
		WHERE ID=#{id}
	</update>

	<delete id="delete" flushCache="true" parameterType="String">
		DELETE from
		t_s_user where id = #{id}
	</delete>
	

	<select id="getByPage" resultMap="BaseResultMap">
		select * from t_s_user
	</select>
	<!--是否启用用户  -->
	<update id="updateUserEnabled" flushCache="true" parameterType="Object">
	UPDATE t_s_user SET enabled=#{enabled} where id=#{id}
	</update>
	<!--是否逻辑删除  -->
	<update id="updateUserDelete" flushCache="true" parameterType="Object">
	UPDATE t_s_user SET deleted=#{deleted} where id=#{id}
	</update>
	<!--修改密码-->
	<update id="updateUserPassword" flushCache="true" parameterType="com.web.entity.User">
		UPDATE t_s_user SET password=#{password} where id=#{id}
	</update>

</mapper>